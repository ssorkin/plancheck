# Data is never committed: rebuild it with `pc acquire && pc ingest && pc geocode`,
# verified against the checksums in manifests/.
data/

# Python
__pycache__/
*.pyc
.venv/
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# OS / editor
.DS_Store
.claude/settings.local.json
.env
